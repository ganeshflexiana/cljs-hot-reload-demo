# ClojureScript Hot Reload Demo

ClojureScript setup using Shadow-CLJS, Reagent, and React with hot reloading.

## Run Locally

```bash
npm install
npm install --save-dev shadow-cljs
npm install react react-dom process
npx shadow-cljs watch app
